// Format version constants. Kept free of zod so the renderer can import them
// without pulling the schema runtime into its bundle.
export const VERSION_NUMBER = 7.0;
export const LEGACY_VERSION = 5.1;
